# own_docker_project

To setup the rootfs, run this script:

```
chmod +x setup_rootfs.sh && ./setup_rootfs.sh
```

Then you can run this python code, to run the simple the simple container:
```
sudo python3 src/main.py
```